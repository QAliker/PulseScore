// WebSocket support was removed in the migration to API-Football v3 (RapidAPI).
// The client now uses REST-only via the get() method.
describe('ApiFootballClient — WebSocket (removed)', () => {
  it('placeholder: WebSocket tests removed after REST migration', () => {
    expect(true).toBe(true);
  });
});
